sessions/
dist/
dist.bak/
.dev/
node_modules/
/config.json
/.vscode
/diagnostics
note
.DS_Store
.playwright-chromium-installed
.stfolder/
.env

# --- local tooling / build artifacts (added during RewardsManager dev) ---
.vs/
**/bin/
**/obj/
icon-preview.png
geometry.txt

# autorun runtime state (not source)
/autorun/logs/
/autorun/.last-run-date
/autorun/update-status.json
/autorun/.run-lock
/autorun/*.pdb
# 例外：随仓库分发的预编译管理程序（framework-dependent，目标机需装 .NET 8 桌面运行时）
!autorun/RewardsManager.exe

# --- 工作区本地记忆 / 技能（WorkBuddy 工作区数据，不入库）---
/.workbuddy/

# --- release 构建产物（zip 仅作为 GitHub Release Asset 发布，不入库）---
/release/

# --- 临时路径探测测试目录（调试用，非项目源码）---
scripts/_pathtest/
scripts/_updtest/